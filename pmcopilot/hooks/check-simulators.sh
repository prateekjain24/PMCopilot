#!/bin/bash
# PMCopilot Hook: SessionStart
# Checks if iOS Simulator (xcrun simctl) and/or Android Emulator (adb)
# are available. Outputs context about available devices.
# Exit 0 = allow session, Exit 2 = block (reason on stderr)

# TODO: Implement simulator/emulator availability check (Phase 4)
exit 0
